{
  "name": "@pagaska/upload-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Internal Pagaska upload engine — resumable, chunked, with independent retry pool. Treat as an existing package; do not modify.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./config": "./src/config/defaultConfig.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
