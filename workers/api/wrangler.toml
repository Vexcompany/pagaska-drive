name = "pagaska-api"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Public environment variable. Secrets (client id/secret, refresh
# token, jwt secret) are set via:
#   wrangler secret put NAME
[vars]
CORS_ORIGIN = "*"
