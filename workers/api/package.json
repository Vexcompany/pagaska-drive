{
  "name": "@pagaska/worker-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker REST API for Pagaska Drive.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .wrangler"
  },
  "dependencies": {
    "@pagaska/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0",
    "wrangler": "^3.78.0"
  }
}
